# 2026-06-13

## 当前阶段

- 阶段：Phase 1
- 状态：进行中
- 开始日期：2026-06-13
- 最近更新：2026-06-13

## 已完成

- 为关键主链路补充高价值代码注释，覆盖入口工厂、状态机、采集适配器、PCM 转换、诊断页和工作台。
- 新增并整理 `docs` 目录，将执行链路文档和长期主计划迁移到 `docs/architecture`、`docs/plans`。
- 统一项目内跨目录导入为 `@/...`，补齐 `vite`、`vitest`、TypeScript 对别名的支持。
- 对 `src/capture` 做粗粒度重构，拆分为适配器入口、采集图构建、采集会话三层。
- 收敛 `RecorderController` 内部重复的 runtime/summary/event 上下文同步逻辑。
- 为新 `capture` 结构补充单元测试，覆盖 `BrowserCaptureSession` 生命周期与 `createCaptureGraph` 降级链路。
- 清理一轮无价值抽象，移除 `BrowserCaptureAdapter` 内对 `createCaptureGraph` 的空转发包装，并收缩 controller 中只用一次的状态同步 helper。
- 收敛 `playground` 中重复的 `try/catch + appendLog + render` 操作模板，改为统一动作执行入口。
- 合并 capture 层内部的 warning/error 上报通道，统一为 `issue` 事件；warning 直接 `console.warn`，warning/error 都通过 EventBus 广播。

## 进行中

- 继续按 Phase 1 现有边界收敛测试覆盖。
- 评估 `playground` 和根页诊断是否需要在后续 phase 再做结构调整。

## 下一步

- 结合 upstream 行为继续补充更贴近真实浏览器环境的功能测试。
- 在 Phase 2 开始前规划 `buffer / processing / encoding` 的目录边界。

## 风险/阻塞

- 当前 `capture-graph` 的 `AudioWorklet` 主路径仍主要依赖静态实现与人工验证，缺少浏览器级自动化覆盖。
- `playground/main.ts` 和 `src/main.ts` 仍偏重，但目前属于示例/诊断层，暂未纳入本轮重构重点。

## Capture 粗粒度重构

- 目标：在不把结构拆得过碎的前提下，降低 `capture` 层复杂度，并为后续 phase 的编码、缓冲、插件接入预留清晰挂点。
- 完成：
  - 从原 `src/capture/browser-capture-adapter.ts` 中抽出 `BrowserCaptureSession` 到 `src/capture/browser-capture-session.ts`。
  - 抽出 `createCaptureGraph` 到 `src/capture/capture-graph.ts`，集中承载 `AudioWorklet` 注册和 `ScriptProcessor` 降级逻辑。
  - 将 `BrowserCaptureAdapter` 收敛为“输入流选择 + AudioContext 创建 + graph/session 装配”的稳定入口。
  - 对 `RecorderController` 增加 `resetSessionTracking`、`syncRuntimeFromSession`、`syncRuntimeFromFrame`、`syncSummaryFromSession`、`syncStoppedSummary`、`createEventContext`，减少状态机样板代码重复。
- 变更文件：
  - `src/capture/browser-capture-adapter.ts`
  - `src/capture/browser-capture-session.ts`
  - `src/capture/capture-graph.ts`
  - `src/core/recorder-controller.ts`
  - `docs/architecture/execution-chain.md`
  - `logs/2026-06-13.md`
- 测试结果：
  - `npm run typecheck` 通过
  - `npm run test:unit` 通过
  - `npm run build` 通过
- 遗留问题：
  - 还缺少对 `AudioWorklet` 主路径的浏览器级覆盖。
- 下次恢复入口：
  - 从 `capture` 层测试继续补强，优先校准更真实的浏览器链路与功能测试边界。

## Capture 层测试补强

- 目标：为本轮粗粒度重构后的 `capture` 结构补上直接单测，降低后续 phase 在采集层继续演进时的回归风险。
- 完成：
  - 新增 `tests/unit/browser-capture-session.spec.ts`，覆盖：
    - 仅在 `recording` 状态接收帧
    - 声道调整 warning 只发一次
    - `close()` 对自持有轨道的释放行为
    - 非法生命周期迁移拦截
  - 新增 `tests/unit/capture-graph.spec.ts`，覆盖：
    - `AudioWorkletNode` 不可用时降级到 `ScriptProcessor`
    - `ScriptProcessor` 回调向绑定 session 转发帧
    - `deactivateCaptureNode()` 可正确移除处理器
  - 同步更新架构文档，明确 `BrowserCaptureAdapter / BrowserCaptureSession / createCaptureGraph` 的分工。
- 变更文件：
  - `tests/unit/browser-capture-session.spec.ts`
  - `tests/unit/capture-graph.spec.ts`
  - `docs/architecture/execution-chain.md`
  - `logs/2026-06-13.md`
- 测试结果：
  - 待执行 `npm run test:unit / npm run build / npm run typecheck`
- 遗留问题：
  - 当前测试仍以 stub 为主，后续应增加浏览器环境下对 `AudioWorklet` 主路径的更真实验证。
- 下次恢复入口：
  - 从功能测试层继续补 `capture` 主路径验证，再决定是否需要扩展到 `playwright` 侧诊断断言。

## 冗余收敛

- 目标：清理“为了分层而分层”的无价值包装和只用一次的 helper，保持结构边界但降低阅读成本。
- 完成：
  - 删除 `BrowserCaptureAdapter` 中仅负责 `return createCaptureGraph(...)` 的空包装方法，改为直接调用采集图构建函数。
  - 将 `capture-graph` 对 `BrowserCaptureSession` 的强类型依赖收敛为最小 `acceptFrame()` 能力接口，减少不必要耦合。
  - 删除 `AudioWorkletProcessorMessage` 中未被消费的 `frameLength` 字段。
  - 将 `RecorderController` 中只用一次的 `resetSessionTracking`、`syncRuntimeFromFrame`、`syncSummaryFromSession`、`syncStoppedSummary` 收回主流程，保留真正复用的 runtime 同步和事件上下文拼装逻辑。
  - 将 `playground/main.ts` 中重复的动作执行模板收敛为 `runLoggedAction()`，减少按钮行为里的重复异常处理和 `render()` 调用。
  - 内联 `src/main.ts` 里只用于一次简单等待的 Promise 包装写法，去掉无收益的块级模板噪音。
- 变更文件：
  - `src/capture/browser-capture-adapter.ts`
  - `src/capture/capture-graph.ts`
  - `src/core/recorder-controller.ts`
  - `playground/main.ts`
  - `src/main.ts`
  - `docs/architecture/execution-chain.md`
  - `logs/2026-06-13.md`
- 测试结果：
  - 待执行 `npm run test:unit / npm run typecheck / npm run build`
- 遗留问题：
  - `capture-graph` 仍然是采集层最复杂的地方，但其中的大部分复杂度来自浏览器能力分支，本身不是无效抽象。
- 下次恢复入口：
  - 继续按“只清理冗余、不打散稳定边界”的原则审查 demo 与诊断层代码。

## Warning 通道收敛

- 目标：消除 warning 与 error 并行走两条对外链路的冗余，统一 capture 层内部问题上报入口，并让 warning / error 作为同类 issue 走事件总线。
- 完成：
  - 将 `CaptureHandlers` 的 `onWarning / onError` 合并为单一 `onIssue` 通道。
  - 删除 `warning` / `error` 分裂的对外事件模型，改为统一 `issue` 事件。
  - warning 在控制器内部直接 `console.warn`，同时和 error 一起通过 `EventBus` 广播 `issue`。
  - `playground` 改为只监听 `issue`，统一渲染 warning 与 error 日志。
  - 更新单测，新增“capture issue 为 error 时进入 issue 事件”的覆盖，确保新模型闭环。
- 变更文件：
  - `src/types.ts`
  - `src/capture/types.ts`
  - `src/index.ts`
  - `src/core/recorder-controller.ts`
  - `src/capture/browser-capture-session.ts`
  - `src/capture/capture-graph.ts`
  - `playground/main.ts`
  - `tests/unit/recorder-controller.spec.ts`
  - `tests/unit/browser-capture-session.spec.ts`
  - `tests/unit/capture-graph.spec.ts`
  - `README.md`
  - `docs/architecture/execution-chain.md`
  - `docs/plans/recorder-ts-master-plan.md`
  - `logs/2026-06-13.md`
- 测试结果：
  - `npm run test:unit` 通过，4 个测试文件 / 11 个用例
  - `npm run typecheck` 通过
  - `npm run build` 通过
- 遗留问题：
  - `AudioWorklet` 主路径仍缺少浏览器级自动化验证，当前 issue 语义主要由 stub 和人工链路验证保障。
- 下次恢复入口：
- 继续在不引入多余事件语义的前提下收敛 demo 层状态和 issue 展示逻辑。

## Warning / Error 语义纠偏

- 目标：按当前架构预期收敛 warning 与 error，不再引入额外 logger 抽象，也不再让问题事件分裂成总线内外两条路径。
- 完成：
  - 修正根入口类型导出，删除残留的 `RecorderErrorEvent`、`RecorderLogger` 导出。
  - 回收长期计划文档中旧的 `warning/error` 事件表述，统一为 `issue` 事件模型。
  - 清理当日日志中被否掉的 `logger 输出 warning`、`EventBus 仅保留 error` 描述，保持代码、文档、日志一致。
- 变更文件：
  - `src/index.ts`
  - `docs/plans/recorder-ts-master-plan.md`
  - `logs/2026-06-13.md`
- 测试结果：
  - 待执行 `npm run test:unit / npm run typecheck / npm run build`
- 遗留问题：
  - 需再扫一遍仓库，确认不存在旧的 `warning/error` 对外事件和 rejected logger 语义残留。
- 下次恢复入口：
  - 从全仓关键字扫描和构建验证继续，确认 issue 模型已完全闭环。

## Capture 测试覆盖补强

- 目标：在不继续拆分 capture 结构的前提下，补齐 `browser-capture-adapter` 和 `capture-graph` 的关键分支覆盖，优先降低后续 phase 改动的回归风险。
- 完成：
  - 新增 `tests/unit/browser-capture-adapter.spec.ts`，覆盖外部流复用、`getUserMedia` 约束透传、`AudioContext/webkitAudioContext` 选择、无音轨输入和缺失浏览器能力错误。
  - 扩展 `tests/unit/capture-graph.spec.ts`，覆盖 `AudioWorklet` 注册仅一次、worklet 帧回流、worklet 错误上报、注册失败后的 `ScriptProcessor` 降级。
  - 同步 README 与执行链路文档，明确 capture 层当前自动化覆盖重点。
- 变更文件：
  - `tests/unit/browser-capture-adapter.spec.ts`
  - `tests/unit/capture-graph.spec.ts`
  - `README.md`
  - `docs/architecture/execution-chain.md`
  - `logs/2026-06-13.md`
- 测试结果：
  - 待执行 `npm run test:unit / npm run typecheck / npm run build`
- 遗留问题：
  - 仍缺真实浏览器环境下对 `AudioWorklet` 主路径的功能级验证，当前新增覆盖仍以 node + stub 的单测为主。
- 下次恢复入口：
  - 从 coverage 结果继续看 `capture-graph.ts` 剩余未覆盖分支，再决定是否值得补浏览器功能测试。

## Playground 解耦与 Vue CDN 化

- 目标：移除根目录下的 demo 代码，把示例统一收敛到 `playground`，并确保 playground 只依赖库打包产物而不是工程源码。
- 完成：
  - 删除 `src/main.ts` 和原 `playground/main.ts` 源码耦合 demo。
  - 新增根页 `index.html`，只保留说明和跳转，不再承载诊断逻辑。
  - 将 `playground` 改为静态 HTML + CDN Vue ESM + `playground/app.js` 的轻量示例页。
  - `playground/app.js` 改为直接 import `/dist/index.js`，验证打包产物对外行为。
  - 调整 `dev:playground` 与 Playwright 功能测试链路，先构建库再启动页面。
  - 同步 README、执行链路文档和当日日志，明确 playground 已与工程源码解耦。
- 变更文件：
  - `index.html`
  - `playground/index.html`
  - `playground/app.js`
  - `playground/style.css`
  - `src/main.ts`
  - `playground/main.ts`
  - `package.json`
  - `playwright.config.ts`
  - `tests/functional/app.spec.ts`
  - `README.md`
  - `docs/architecture/execution-chain.md`
  - `logs/2026-06-13.md`
- 测试结果：
  - 待执行 `npm run test:unit / npm run typecheck / npm run build / npm run test:functional`
- 遗留问题：
  - CDN 依赖意味着功能测试和手工示例都要求浏览器可访问外网资源。
- 下次恢复入口：
  - 从构建与功能测试验证继续，确认 `playground` 确实只依赖 `dist/index.js`。
