const app = document.querySelector<HTMLDivElement>("#app")

if (app) {
  app.innerHTML = `
    <main>
      <h1>audio-recorder</h1>
      <p>Phase 0 scaffold is ready.</p>
      <p>See <code>Recorder-TS-Master-Plan.md</code> for the long-term plan.</p>
    </main>
  `
}
