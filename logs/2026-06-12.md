# 2026-06-12

## 当前阶段

- 阶段：Phase 1
- 状态：进行中
- 开始日期：2026-06-12
- 最近更新：2026-06-12

## 已完成

- 完成 `vite`、`commitlint`、`husky` 配置补齐
- 完成 Phase 1 第一版录制主链路
- 下载并解压 upstream Recorder 到 `vendor/Recorder-master` 作为实现参照
- 新增 `/playground/` 手工测试工作台
- 将 Playwright 改为本地默认可见、CI 可无头，并保留显式 headed 脚本
- 删除重复的 `IMPLEMENTATION-LOG.md`，统一改为维护 `logs/YYYY-MM-DD.md`
- 将“Deprecated API 仅作降级路径、命名与封装约束、vendor 仅作参照不复刻”写入主方案文档
- 将浏览器采集实现调整为 `AudioWorklet` 优先、`ScriptProcessor` 降级
- 将 playground 改为中文示例工作台

## 进行中

- 继续按 upstream 实际行为校准 Phase 1
- 补充更接近真实麦克风链路的功能测试策略

## 下一步

- 优先继续 Phase 1 的行为对齐和功能测试收敛
- 然后进入 Phase 2 的 `PcmBufferStore`、重采样、PCM/WAV 导出

## 风险/阻塞

- 当前沙箱环境中 `playwright test` 结果为通过，但 CLI 进程存在收尾卡住现象，需要继续确认 runner 行为
- `AudioWorklet` 已作为当前主路径，但移动端和不同浏览器上的稳定性仍需继续做真实环境验证

## 工具链增强

- 目标：参考 `media-studio` 补齐 `vite`、`commitlint`、`husky` 相关配置，并保持当前单仓库库工程边界不变。
- 完成：
  - 为工程新增 `commitlint` 配置文件。
  - 为工程新增 `husky` 的 `pre-commit` 和 `commit-msg` 钩子。
  - 在 `package.json` 中补充 `prepare`、`commitlint` 脚本和相关开发依赖。
  - 按库工程场景增强 `vite.config.ts`，补充 `alias`、`sourcemap`、`target` 和清理输出目录配置。
- 变更文件：
  - `package.json`
  - `commitlint.config.js`
  - `vite.config.ts`
  - `.husky/pre-commit`
  - `.husky/commit-msg`
  - `logs/2026-06-12.md`
- 测试结果：
  - `npm install` 完成，`prepare` 已执行 `husky`
  - `npm run format` 通过
  - `npm run lint` 通过
  - `npm run typecheck` 通过
  - `npm run test:unit` 通过
  - `npm run test:functional` 通过
  - `npm run build` 通过
  - `npx commitlint --print-config text` 可正常解析配置
- 遗留问题：
  - 终端查看中文 Markdown 仍可能出现乱码，后续以编辑器 UTF-8 显示结果为准。
- 下次恢复入口：
  - 从 `Recorder-TS-Master-Plan.md` 的 Phase 1 录制主链路开始。

## Phase 1 录制主链路

- 目标：落地 Phase 1 的最小可用录制核心，包括生命周期 API、采集适配、实时 PCM 帧分发、状态事件和声道实际值回写。
- 完成：
  - 新增 `RecorderController`、`EventBus`、`FrameDispatcher`、`BrowserCaptureAdapter` 和录音核心类型定义。
  - 建立 `AudioFrame` 模型与 Float32 -> Int16 PCM 标准化工具。
  - 根入口改为导出 `createRecorder` 和控制器相关类型，不再保留 Phase 0 占位导出。
  - 将首页切换为 Phase 1 状态页，增加基于外部 `MediaStream` 的诊断流程。
  - 为 E2E 验证补充确定性 `CaptureAdapter` 路径，确保可稳定观察状态流转与双声道输出信息。
  - 补齐 `RecorderController` 与 `audio-frame` 单元测试，更新功能测试断言。
  - 修正 `playwright.config.ts` 中依赖宿主机 Node 路径的问题。
- 变更文件：
  - `src/index.ts`
  - `src/types.ts`
  - `src/capture/types.ts`
  - `src/capture/browser-capture-adapter.ts`
  - `src/core/event-bus.ts`
  - `src/core/frame-dispatcher.ts`
  - `src/core/recorder-controller.ts`
  - `src/utils/audio-frame.ts`
  - `src/main.ts`
  - `src/style.css`
  - `tests/unit/recorder-controller.spec.ts`
  - `tests/unit/audio-frame.spec.ts`
  - `tests/functional/app.spec.ts`
  - `playwright.config.ts`
  - `README.md`
  - `logs/2026-06-12.md`
- 测试结果：
  - `.\\node_modules\\.bin\\eslint.cmd .` 通过
  - `.\\node_modules\\.bin\\tsc.cmd -p tsconfig.build.json --noEmit` 通过
  - `.\\node_modules\\.bin\\vitest.cmd run --coverage` 通过
  - `.\\node_modules\\.bin\\vite.cmd build` 通过
  - `node .\\scripts\\verify-exports.mjs` 通过
  - 独立 `chromium` 脚本访问 `http://127.0.0.1:4173/?diagnostic=e2e`，诊断状态为 `External stream diagnostic passed.`
  - `playwright test` 在当前沙箱内 `.last-run.json` 结果为 `passed`，但 CLI 进程未正常退出并触发超时
- 遗留问题：
  - 当前沙箱环境中 `playwright test` 存在 runner 收尾卡住问题，需后续继续确认是否为 Chrome channel 或进程清理行为导致。
  - `BrowserCaptureAdapter` 当前以 `ScriptProcessor` 作为 Phase 1 采集主实现，后续可按主文档继续评估 `AudioWorklet`。
- 下次恢复入口：
  - 继续完善 Phase 1 的真实浏览器录音功能测试策略；完成后进入 Phase 2 的 `PcmBufferStore`、重采样、PCM/WAV 导出。

## Playground 与参照源调整

- 目标：修正 Playwright 的可见性预期，新增独立 playground 页面，并将 upstream Recorder 作为后续实现参照，同时去掉重复日志文件。
- 完成：
  - 确认 `channel: "chrome"` 正常可用，Playwright 能正常拉起可见 Chrome；此前“未打开浏览器”是 `headless: true` 配置导致。
  - 将 Playwright 改为本地默认 `headed`，通过 `PW_HEADLESS=1` 或 CI 环境进入无头模式，并新增 `test:functional:headed` 脚本。
  - 新增 `playground/` 工作台页面，支持麦克风和外部音调流两种输入、生命周期按钮、实时状态、日志和运行时快照。
  - 根页面增加跳转到 playground 的入口。
  - 下载并解压 upstream Recorder 到 `vendor/Recorder-master`，后续功能实现以其源码与 demo 为参照，不再仅依赖规划文档。
  - 将日志约束改为只维护 `logs/YYYY-MM-DD.md`，不再维护单独的 `IMPLEMENTATION-LOG.md`。
- 变更文件：
  - `package.json`
  - `playwright.config.ts`
  - `src/main.ts`
  - `src/style.css`
  - `playground/index.html`
  - `playground/main.ts`
  - `playground/style.css`
  - `README.md`
  - `Recorder-TS-Master-Plan.md`
  - `logs/2026-06-11.md`
  - `logs/2026-06-12.md`
  - `eslint.config.js`
  - `tsconfig.json`
  - `vendor/Recorder-master/**`
- 测试结果：
  - `node --input-type=module` 直接调用 Playwright，`channel: "chrome"` 的 `headless: true/false` 启动都成功
  - upstream 已成功下载并解压到 `vendor/Recorder-master`
  - `.\\node_modules\\.bin\\eslint.cmd .` 通过
  - `.\\node_modules\\.bin\\tsc.cmd -p tsconfig.build.json --noEmit` 通过
  - `.\\node_modules\\.bin\\vitest.cmd run --coverage` 通过
  - `.\\node_modules\\.bin\\vite.cmd build` 通过
  - `node .\\scripts\\verify-exports.mjs` 通过
  - 独立 `chromium` 脚本访问 `http://127.0.0.1:4173/playground/`，页面标题与控件渲染正常
- 遗留问题：
  - playground 已是独立设计，不照搬 upstream，但后续需要随着库能力增加继续扩展示例
- 下次恢复入口：
  - 从 upstream `recorder-core.js` 的 `open/start/resume/envIn/stop` 行为继续对齐 Phase 1，并把真实麦克风场景纳入功能测试

## 方案约束与采集主路径调整

- 目标：把本轮新增约束落入主方案文档，并将浏览器采集主实现从过时 API 调整为现代 API 优先，同时把 demo 切换为中文。
- 完成：
  - 在 `Recorder-TS-Master-Plan.md` 中补充实施约束，明确：
    - upstream vendor 仅作行为参照，不以复刻为目标；
    - 不能仅凭方案文档推断行为；
    - `Deprecated` 浏览器 API 只作为低版本降级路径；
    - 命名、变量新增、私有字段与公有 API 的规范要求；
    - 避免单点调用的一行函数式过度封装。
  - 将 `BrowserCaptureAdapter` 调整为 `AudioWorklet` 优先，并在不可用时显式发出 `script-processor-fallback` 告警后回退到 `ScriptProcessor`。
  - 保留 `channel-count-adjusted` 的实际帧级校正逻辑，避免仅依据 `track settings` 提前误报。
  - 将 `/playground/` 的标题、说明、控制项和日志文案改为中文。
  - 更新 `README.md`，同步当前“现代 API 优先、中文示例工作台”的项目状态。
- 变更文件：
  - `src/capture/browser-capture-adapter.ts`
  - `playground/main.ts`
  - `playground/index.html`
  - `src/main.ts`
  - `README.md`
  - `Recorder-TS-Master-Plan.md`
  - `logs/2026-06-12.md`
- 测试结果：
  - 待执行 `lint / typecheck / test:unit / build`
- 遗留问题：
  - 仍需补做 `AudioWorklet` 主路径的浏览器级验证和更真实的麦克风链路测试。
- 下次恢复入口：
  - 先完成本轮静态与单元校验，再继续对齐 upstream `open/start/resume/envIn/stop` 行为细节。

## 按约束回扫与代码收敛

- 目标：根据根目录文档重新扫描当前实现，收紧公共类型、状态语义、构建产物和示例页调用方式，使其更符合实现约束、工程要求与代码规范。
- 完成：
  - 将 `CreateRecorderOptions` 中的 `captureAdapter` 改为明确的 `CaptureAdapter` 类型，去掉弱化的 `unknown` 输入。
  - 收紧 `EventBus` 泛型边界，整理事件监听器存储结构，减少依赖宽泛断言的实现细节。
  - 将 `RecorderController` 内部状态字段重命名为更直接的领域语义，如 `captureSession`、`recorderState`、`sessionRuntimeInfo`、`latestSessionSummary`。
  - 调整 `BrowserCaptureAdapter`，保持仅在实际帧到达后根据真实输入声道做 `channel-count-adjusted` 校正，避免依赖 `MediaStreamTrack.getSettings()` 提前误报麦克风声道。
  - 将 `BrowserCaptureSession` 和首页诊断用的 `DeterministicCaptureSession` 从互斥布尔状态改为单一状态枚举，避免 `isClosed` / `isRecording`、`closed` / `running` 这类并存状态字段继续存在。
  - 连续 3 轮按文档约束回扫代码，收敛公共运行时边界：为 `RecorderState`、`RecorderWarningCode`、`RecorderInputSource` 增加共享常量值定义，并在 controller、capture、playground、测试中复用，减少 magic string 散落。
  - 删除单点转发的 `FrameDispatcher` 薄封装，避免“为了抽取而抽取”的无效中间层。
  - 移除声明但未实现的 `channel-count-clamped` warning code，保证类型边界和当前实现一致。
  - 为 `RecorderController` 补充非法状态迁移测试，验证状态机不仅有类型声明，也会在运行时拦截不合法调用。
  - 为 `ScriptProcessor` 降级路径补充明确注释，保持“过时 API 仅作 fallback”的实现意图。
  - 去掉 `playground/main.ts` 中的 `@ts-ignore`，改为显式使用 `RecorderOpenOptions`。
  - 为 `build` 补上类型声明产物输出，将 `package.json` 的 `exports` 改为同时声明 `import` 与 `types`，并增强 `scripts/verify-exports.mjs` 的一致性校验。
  - 在用户反馈麦克风场景误报后，回退基于 `getSettings().channelCount` 的提前判定，恢复为原先更稳妥的帧级确认逻辑。
- 变更文件：
  - `src/index.ts`
  - `src/types.ts`
  - `src/core/event-bus.ts`
  - `src/core/recorder-controller.ts`
  - `src/capture/browser-capture-adapter.ts`
  - `playground/main.ts`
  - `tests/unit/recorder-controller.spec.ts`
  - `tsconfig.build.json`
  - `package.json`
  - `scripts/verify-exports.mjs`
  - `logs/2026-06-12.md`
- 测试结果：
  - 当前沙箱环境直接执行 `npm run lint`、`npm run typecheck` 时，Node CLI 会因宿主目录 `C:\\Users\\chens` 的 `EPERM`/`lstat` 限制而失败，待提权后复跑完整校验。
- 遗留问题：
  - 仍需提权执行 `lint / typecheck / test / build / verify:exports` 做最终确认。
  - 当前库仍处于 Phase 1，尚未进入主文档要求的 `PcmBufferStore`、PCM/WAV 导出与子路径入口阶段。
- 下次恢复入口：
  - 提权完成工具链校验后，继续按 Phase 2 落地缓冲、重采样和 PCM/WAV 导出。

## Phase 1 定稿整理

- 目标：修正 `playground` 的类型问题，完成 Phase 1 录音核心链路的定稿整理，并以当前阶段边界提交一版稳定代码。
- 完成：
  - 修正 `playground/main.ts` 对 recorder 事件负载、共享枚举和运行时快照的类型使用，确保与当前事件模型一致。
  - 补充 `EventBus.off()`，并让 `RecorderController` 对外提供显式移除监听能力。
  - 将 recorder 事件负载统一为带 `controller/sessionId/emittedAt/runtimeInfo/summary` 的上下文对象，方便业务监听后直接操作。
  - 将 `RecorderState`、`RecorderWarningCode`、`RecorderInputSource` 统一收敛为真正的 `enum`，并同步到 controller、capture、playground、测试和导出入口。
  - 将 capture 层和根页诊断层的局部会话状态枚举提升为共享 `CaptureSessionState`，避免状态定义散落。
- 变更文件：
  - `playground/main.ts`
  - `src/types.ts`
  - `src/index.ts`
  - `src/core/event-bus.ts`
  - `src/core/recorder-controller.ts`
  - `src/capture/browser-capture-adapter.ts`
  - `src/main.ts`
  - `tests/unit/recorder-controller.spec.ts`
  - `tests/functional/app.spec.ts`
  - `logs/2026-06-12.md`
- 测试结果：
  - `npm run typecheck` 通过
  - 待执行 `lint / test:unit / build`
- 遗留问题：
  - 当前 Phase 1 仍未进入 vendor 对齐中更关键的 `PcmBufferStore`、重采样、PCM/WAV/MP3 导出与 `onProcess` 主链。
- 下次恢复入口：
  - 从 Phase 2 的缓冲与导出链路开始，优先补 `PcmBufferStore + PCM/WAV 导出`。
